{
  "name": "@uztt/shared",
  "version": "0.1.0",
  "description": "UZTT — umumiy tiplar, socket kontraktlari, enumlar",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "test": "echo \"no tests\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
